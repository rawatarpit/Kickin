<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <user-permission android:name="andriod.permission.INTERNET" />
    <user-permission android:name="andriod.permission.ACCESS_NETWORK_STATE" />
    <user-permission android:name="andriod.permission.READ_MEDIA_IMAGES" />
    <user-permission android:name="andriod.permission.READ_MEDIA_VIDEO" />
    <user-permission android:name="andriod.permission.READ_EXTERNAL_STORAGE" />
    <user-permission android:name="andriod.permission.WRITE_EXTERNAL_STORAGE" />
    <user-permission android:name="andriod.permission.MANAGE_EXTERNAL_STORAGE" />
    <user-permission android:name="andriod.permission.VIBRATE" />

    
    <application
        android:label="${appName}"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalstorage="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="com.google.firebase.messaging.default_notifications_channel_id"
              android:value="chat_channel"
              />    
             <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>        
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data
                android:scheme="io.superbase.flutterquickstart"
                android:host="login-callback" />
            </intent-filter>
            <intent-filter>
                <action android:name="FLUTTER_NOTIFIACTIONS_CLICK"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>

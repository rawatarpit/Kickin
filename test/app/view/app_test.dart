import 'package:flutter_test/flutter_test.dart';

void main() {
  group('App', () {
    testWidgets('renders HomePage', (tester) async {
      // Uncomment and replace `KickinApp` and `HomePage` with your
      //actual widget names
      // await tester.pumpWidget(const KickinApp());
    });
  });
}

// ignore_for_file: prefer_const_constructors
import 'package:kickin_blocks/kickin_blocks.dart';
import 'package:test/test.dart';

void main() {
  group('KickinBlocks', () {
    test('can be instantiated', () {
      expect(KickinBlocks(), isNotNull);
    });
  });
}
